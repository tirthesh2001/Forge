# Vite exposes only vars prefixed with VITE_. Copy to Vercel: Project → Settings → Environment Variables (apply to Production + Preview).
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here
